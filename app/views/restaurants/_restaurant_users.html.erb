<%- restaurant_users = @users.map { |user| { id: user.id, email: user.email, role: user.role_in_resource(@restaurant) } } %>

<%= react_component("RestaurantUsers", { restaurant_id: @restaurant.id, restaurant_users: restaurant_users }) %>
